from setuptools import setup

import os
BASEPATH = os.path.dirname(os.path.abspath(__file__))

setup(name='organic_enzyme_rxn_classifier',
    py_modules=['organic_enzyme_rxn_classifier'],
    install_requires=[],
)
